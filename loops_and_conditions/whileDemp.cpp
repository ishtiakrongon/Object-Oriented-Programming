// While loop is used for when we don't know when the loop will end
// when there is a certain conditions need to follow
#include <iostream>
using namespace std;

int main() {
    
    int n = 99;

    while(n != 0) {
        cin >> n;
    }
    cout << n << endl;

    return 0;
}